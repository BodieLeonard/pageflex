PageFlex HTML5 Responsive Theme
================================

dependencies:
	gulp
	nodejs
	bower
	sass
	compass

to build:
	run $ gulp

to deploy:
	run $ gulp and push the /public/Inserts/SiteHeader.html to Pageflex FTP